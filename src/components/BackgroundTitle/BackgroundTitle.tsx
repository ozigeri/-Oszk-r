import React from 'react';
import './BackgroundTitle.scss';

const BackgroundTitle: React.FC = () => {
    return( 
        <div className="background-title">
            OSZKÁR VROOM VROOM
        </div>
    );
};

export default BackgroundTitle;
